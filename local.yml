---
- hosts: localhost
  connection: local
  become: true

  tasks:
  - name: install de
    package:
      name:
        - sway
        - waybar
        - alacritty
        - grim
        - wofi
  - name: set config
    copy:
      src: config
      dest: ~/.config
  
