---
- hosts: localhost
  connection: local
  become: true

  tasks:
  - name: install de
    package:
      name:
        - sway
        - waybar
        - alacritty
        - grim
        - wofi
  - name: set config
    copy:
      src: dotfiles/
      dest: /home/wyatt/.config
  - name: setup dev environment
    package:
      name:
        - rust
  #script: scripts/amp.sh
  #args:
    #executable: zsh
  
